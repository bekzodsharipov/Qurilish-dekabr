document.addEventListener("DOMContentLoaded",(()=>{setTimeout((()=>{document.querySelectorAll(".minifyImg").forEach((e=>{const t=e.getAttribute("data-src");if(!t)return;const n=new Image;n.src=t,n.onload=()=>{e.src=t,e.classList.remove("blur-up")},n.onerror=()=>{console.warn("Rasm yuklanmadi:",t)}}))}),1e3)}));
let totalSeconds = 60; const timerElement = document.getElementById("timer"); const countdown = setInterval(() => { if (totalSeconds <= 0) { clearInterval(countdown); return; } totalSeconds--; let minutes = Math.floor(totalSeconds / 60); let seconds = totalSeconds % 60; let formattedMinutes = minutes.toString().padStart(1, "0"); let formattedSeconds = seconds.toString().padStart(2, "0"); timerElement.textContent = `0${formattedMinutes}:${formattedSeconds}`; }, 1000); 
